<section class="ch-hero ch-hero-card">
  <div class="ch-container">
    <div class="ch-row">
      <div class="ch-hero-content">
        {% if item.hero_title is not empty %}
        <h1>{{ item.hero_title }}</h1>
        {% endif %}

        {% if item.hero_text is not empty %}
        <p>{{ item.hero_text }}</p>
        {% endif %}

          <div class="ch-hero-content-card">

            {% if item.hero_card_title %}
              <h2>{{ item.hero_card_title }}</h2>
            {% endif %}

            {% if item.hero_card_text %}
            <p class="card-subtext">{{ item.hero_card_text }}</p>
            {% endif %}

            {% if item.hero_card_button %}
            <a href="{{item.hero_card_button.url}}" {{item.hero_card_button.target}} class="ch-button ch-button-green ch-button-green-large">{{item.hero_card_button.text}}</a>
            {% endif %}

          </div>
      </div>

        <div class="ch-hero-image ch-hero-card-image">
           <img src="{{TimberImage(item.hero_image).src}}" alt="{{item.hero_image.alt}}" style="max-width:585px"/>
        </div>

    </div>
  </div>
</section>
<div class="ch-hero-spacer"></div>


{# <section class="ch-hero ch-hero-card">
  <div class="ch-container">
    <div class="ch-row">
      <div class="ch-hero-content">

      {% if item.hero_title is not empty %}
        <h1>{{ item.hero_title }}</h1>
      {% endif %}

      {% if item.hero_text is not empty %}
      <p>{{ item.hero_text }}</p>
      {% endif %}

        <div class="ch-hero-content-card">

            {% if item.hero_card_title %}
              <h2>{{ item.hero_card_title }}</h2>
            {% endif %}

            {% if item.hero_card_text %}
            <p class="card-subtext">{{ item.hero_card_text }}</p>
            {% endif %}

            {% if item.hero_card_button %}
            <a href="{{item.hero_card_button.url}}" {{item.hero_card_button.target}} class="ch-button ch-button-green ch-button-green-large">{{item.hero_card_button.text}}</a>
            {% endif %}

          </div>
      </div>

      {% if item.hero_image is not empty %}
        <div class="ch-hero-image ch-hero-card-image">
          <img src="{{TimberImage(item.hero_image).src}}" alt="{{item.hero_image.alt}}" style="max-width:585px"/>
        </div>
      {% endif %}


    </div>
  </div>
</section>
<div class="ch-hero-spacer"></div>
 #}
