{% if item.hero_image is not empty %}
<section class="ch-hero ch-hero-bg" style="background-image: url({{TimberImage(item.hero_image).src}});">
{% else %}
<section class="ch-hero ch-hero-bg">
{% endif %}
  <div class="ch-container">
    <div class="ch-row">
      <div class="ch-hero-content">
        <h1>{{ item.hero_title }}</h1>
        <p>{{ item.hero_text }}</p>
        <a href="{{ item.hero_large_button.url }}" class="ch-button ch-button-green ch-button-green-large">{{ item.hero_large_button.text }}</a>
      </div>
    </div>
  </div>
  <div class="ch-hero-bg-filter"></div>
</section>
