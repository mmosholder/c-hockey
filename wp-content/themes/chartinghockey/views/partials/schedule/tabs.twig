<div class="gc-row">
  <ul class="gc-tabbed-content-tabs">
    {% for event_tab in item.event_tab %}
    <li class="gc-tabbed-content-tab {% if loop.first %}-active -line-right {% else %} -line-left {% endif %}">
      <a class="gc-tabbed-content-marker"
        href="#tab{{ event_tab.tab_id }}">
        <div class="gc-tabbed-content-marker-outer">
          <span class="gc-tabbed-content-marker-inner" />
        </div>
        <span class="gc-tabbed-content-tab-title">{{ event_tab.event_tab_title }}
        </span>
      </a>
    </li>
    {% if loop.first %}
    <li class="gc-tabbed-content-tab-line" /></li>
    {% endif %}
    {% endfor %}
  </ul>
</div>
